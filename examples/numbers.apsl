// Rules of +, - and * on real numbers

#use examples/plus.apsl
#use examples/mult.apsl
#use examples/power.apsl

K :: R ;

_ :: {
    // Distributivity
    A * (B + C) = (A * B) + (A * C) ;
    // Times two
    A * 2 = A + A ;
    // Power of two
    A ^ 2 = A * A ;
}
