Todo List :
- pattern-match functions (just like it is done with the properties)
- add new properties without using a brace-group
- use hashmaps or hashsets instead of vectors in the graph
- optimize the shit out of everything
- can use extensions (...) anywhere in an expression e.g.
    * ... + A * B = ... + (A * B)
- different parsers for generators (separate start & end)
- enforce operator priority or parentheses
- don't check properties on older nodes
	(filter by node-depth, comparing to graph.max_depth)
- new thread for each property-check

Urgent Fix List :
