^ r :: {
    // Null element
    A ^ 0 = 1 ;
    // Sequential definition
    // A ^ N = # * : N : A # ;
    A ^ 2 = A * A ;
}

_ :: {
    (A ^ B) ^ C = A ^ (B * C) ;
    (A ^ K) * (A ^ P) = A ^ (K + P) ;
    (A ^ K) * (B ^ K) = (A * B) ^ K ;
}
