Todo List :
- optimize everything
- different parsers for generators (separate start & end)
- use more references, less copies of data (for threads etc)
- memoization
- write new parsing tests
- only iterate over  graph "outer layer" (doesn't seem to work somehow)
- re-implement generators using a two-argument function
- special values can hbe more than 9 ([0-9]+)
- atom values can be more than one char long
- fixed number of worker threads instead of one thread per match
- implement functions with multiple arguments

Urgent Fix List :
