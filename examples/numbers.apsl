// Rules of +, - and * on real numbers

#use examples/plus.apsl
#use examples/mult.apsl
#use examples/power.apsl

K :: R ;

_ :: {
    // Distributivity
    A * (B + C) = (A * B) + (A * C) ;
    // Times two (todo: eventually replace this by a generator)
    A * 2 = A + A ;
}

// in repl: prove (A + B) ^ 2 = (A ^ 2) + (2 * A * B) + (B ^ 2)
